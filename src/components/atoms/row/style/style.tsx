// Import plugin
import styled from "styled-components";

// Create new style
const Row = styled.div`
  display: flex;
  flex: 0 1 auto;
  flex-wrap: wrap;
  flex-direction: row;
  box-sizing: border-box;
  margin-left: -1rem;
  margin-right: -1rem;
`;

// Export new style
export default Row;
